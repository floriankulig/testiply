import styled from "styled-components";

export const Button = styled.button`
  background-color: white;
  border: none;
  padding: 0.8em 1.8em;
  font-weight: bold;
  font-size: 1.1rem;
  border-radius: 2em;
  cursor: pointer;
`;
